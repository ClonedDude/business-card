{{-- <form action="{{ route('companies.users.store', $company->id) }}" method="POST" class="card">
    <div class="card-header">
        <div class="card-title">
            {{ __('Create User') }}
        </div>
    </div>

    <div class="card-body">
        @csrf
        <x-text-input title="name" name="name" id="name-input" required="required" />

        <x-text-input title="email" name="email" id="email-input" required="required" />

        <x-text-input type="password" title="password" name="password" id="password-input" required="required" />

        <x-text-input type="password" title="password_confirmation" name="password_confirmation"
            id="password-confirmation-input" required="required" />
    </div>
    <div class="card-footer">
        <button class="btn btn-primary">
            Submit
        </button>
    </div>
</form> --}}
<div>
    Div
</div>